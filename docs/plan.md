# 計画（Plan）

## 運用ルール

- この文書は「現在の計画」を表す。過去ログを増やさない。
- 変更履歴は直近10件までとし、重要判断は ADR に移す。
- 自動実行の対象は「Next」のみとする。Backlog は自動で着手しない。
- 何を作るか（仕様・優先順位・進め方）の正本は `PLAN.md`。この文書はその中から
  「次に着手するタスク」を自動実行パイプライン向けに切り出したものであり、
  内容が食い違う場合は `PLAN.md` を優先する。

## 現状（Status）

- フェーズ：山1「土台づくり」ほぼ完了 → 山2「算数・数学ドリル本体」着手前（`PLAN.md` 参照）
- ブロッカー：Firestore の実データ作成は、英理究さんの Firebase プロジェクト作成後に実施
- 直近の重要決定：技術構成を React / Firebase / TypeScript に確定し、進め方は
  `dev-orchestration-template` のワークフローを採用（2026-07-04）

## ロードマップ（概略）

`PLAN.md` の「山」構成をそのまま使う。

| # | 名称                   | 目標                                             | 状態                             |
| - | ---------------------- | ------------------------------------------------ | -------------------------------- |
| 山1 | 土台づくり             | データの入れ物（Firestore）とログインの仕組み     | 進行中（1-1〜1-3完了、1-4一部）  |
| 山2 | 算数・数学ドリル本体   | テンプレート方式の出題・採点・ヒント機能          | 未着手                           |
| 山3 | 仕上げ                 | 保護者ダッシュボード、実機での動作確認            | 未着手                           |
| 山4 | 将来の拡張             | AI文章問題、英語教科                              | 対象外（完成後に別途プラン作成） |

詳細なタスク一覧・優先順位は `PLAN.md` の「2. 優先順位つきタスク一覧」を参照。

## 今月のゴール

- ~~G1 React + Firebase + TypeScript の開発基盤を確立する~~ ✅ 完了
- G2 ログイン→単元選択→出題→採点→ヒントの骨組みを一本通す（PLAN.md タスク2-1〜2-3）
- G3 決定済みの単元（小6・中2）ぶんのテンプレートを揃える（PLAN.md タスク2-4）

## Next（自動実行対象：最大3件）

### N-001 ログイン画面

- 目的：名前タップでログインする子供用画面、保護者PIN画面を作る（PLAN.md タスク2-1）
- 受入条件：
  - 子供が名前をタップするとログイン状態になる
  - 保護者用のPIN入力画面がある
- 依存：なし
- 触る領域：`src/components`, `src/features`

### N-002 単元えらび画面

- 目的：ログインした子供の学年に応じて単元ボタンを出し分ける（PLAN.md タスク2-2）
- 受入条件：
  - 小6・中2それぞれで、決定済みの単元ボタンが表示される
- 依存：N-001
- 触る領域：`src/components`, `src/features`

### N-003 テンプレートエンジン本体（採点・ヒント込み）

- 目的：出題→回答→採点→3段階ヒントまでが動く最小の骨組みを作る（PLAN.md タスク2-3）
- 受入条件：
  - かけ算テンプレートで、出題→回答→正誤判定→ヒント表示が一通り動く
- 依存：N-002
- 触る領域：`src/lib`, `src/features/templates`

## Done（今期完了）

### 開発環境の準備 ✅

- PR: #1（マージ済み）
- 完了：React + TypeScript + Vite の土台、ESLint/Prettier/Vitest 設定
- 完了：CI 品質ゲート（lint/format/typecheck/test/build/policy_check/secret-scan）
- 完了：問題の共通型（`src/types/problem.ts`）、出題窓口（`src/lib/templateEngine.ts`）、
  かけ算テンプレートと自動テスト

## Backlog（保留）

- B-001 1-1で決めた単元ぶんのテンプレート追加（PLAN.md タスク2-4）
- B-002 図形・グラフの描画（PLAN.md タスク2-6）
- B-003 採点・学習記録のFirestore保存（PLAN.md タスク2-7）
- B-004 保護者ダッシュボード（PLAN.md タスク3-1）

## GitHub Issue / Project 対応表

（現時点では Issue 未作成。今後 Issue を切った際にここへ追記する）

## 直近の変更履歴（最大10件）

- 2026-07-04: docs/plan.md をテンプレートのサンプル内容（Python前提・架空のIssue番号）から
  MiraStudy の実際の状態に初期化。Copilot コードレビュー（PR #1）の指摘を受けて対応。
- 2026-07-04: PR #1 作成（React+TypeScript+Vite の開発環境と問題の共通型・かけ算テンプレート）
